# 🐾 GigaPet — Telegram-бот тамагочи

Telegram-бот с виртуальным питомцем-котом, который реагирует на новости, следит за погодой и требует постоянного ухода.

---

## 🚀 Быстрый старт

### 1. Установить зависимости

```bash
pip install -r requirements.txt
```

Основные зависимости: `pyTelegramBotAPI`, `httpx`, `beautifulsoup4`, `Pillow`, `python-dotenv`

### 2. Создать `.env` файл

```env
BOT_TOKEN=...         # Токен из @BotFather
GROQ_API_KEY=...      # API-ключ из https://console.groq.com/keys
LOGS_GROUP_ID=...     # ID группы для логов (опционально)
ADMIN_USER_IDS=[...]  # JSON-список ID администраторов (опционально)
```

### 3. Запустить

```bash
python bot.py
```

---

## 🎮 Возможности

### Питомец
- Создание питомца с именем
- Отслеживание трёх показателей: **Сытость**, **Энергия**, **Настроение** (0–100)
- Внутренняя валюта — монеты (начальный баланс: 100)

### Действия
| Действие | Эффект |
|---------|--------|
| 🍖 Покормить | Восстанавливает сытость |
| 🎮 Поиграть | Улучшает настроение |
| 💤 Спать | Восстанавливает энергию |

### Аксессуары (магазин, 100 монет каждый)
| Аксессуар | Открывает |
|-----------|-----------|
| 💰 Денежный свитер | Финансовые новости (Forbes, RIA, микс) |
| 🎧 Геймерские наушники | Игровые новости (StopGame) |
| ☂️ Погодный зонтик | Текущая погода в Ростове-на-Дону |

На питомце одновременно может быть **один** аксессуар.

### Новости и погода
Питомец читает новости с выбранного источника и эмоционально реагирует через **Groq AI (llama-3.1-8b-instant)**. Реакция влияет на показатель настроения.

**Источники:**
- 📰 RIA — экономика и политика
- 💰 Forbes — финансы и бизнес
- 🎮 StopGame — игровые новости
- 🔄 Микс — несколько источников сразу
- ☀️ Погода — Open-Meteo API (Ростов-на-Дону)

Если Groq API недоступен, работает **локальный fallback** с предустановленными реакциями.

---

## ⏱️ Фоновые задачи

| Задача | Интервал | Действие |
|--------|----------|----------|
| `hourly_decay` | 1 час | Сытость −10, Настроение −5 |
| `energy_recovery` | 30 мин | Энергия +10×(сытость/100), минимум +1 |
| `check_low_stats` | 2 часа | Проверка критических показателей и уведомление |

Уведомления не дублируются — повторное предупреждение отправляется только после восстановления показателя выше порога.

**Пороги уведомлений:** Сытость < 30, Энергия < 20, Настроение < 30.

---

## 🗂️ Структура проекта

```
├── bot.py                  # Основной файл бота
├── tasks.py                # Фоновые задачи
├── image_utils.py          # Генерация изображений (PIL)
├── Modules/
│   ├── __init__.py
│   └── news_module.py      # Парсинг новостей и Groq AI
├── Images/                 # Графические ресурсы (1000×1000 px)
│   ├── Cat.png
│   ├── Food.png, Game.png
│   ├── CatLowEnergy.png, CatLowMood.png, CatLowSatiety.png
│   ├── EnergyIcon.png, MoodIcon.png, SatietyIcon.png
│   └── AcsFinance.png, AcsGaming.png, AcsWeather.png
├── pets.db                 # SQLite база данных (создаётся автоматически)
├── .env                    # Переменные окружения
└── requirements.txt
```

---

## 🗄️ База данных

Таблица `pets` (SQLite, файл `pets.db`):

| Поле | Тип | Описание |
|------|-----|----------|
| `user_id` | TEXT PK | Telegram ID пользователя |
| `name` | TEXT | Имя питомца |
| `money` | INTEGER | Монеты (по умолчанию 100) |
| `satiety` | INTEGER | Сытость 0–100 |
| `energy` | INTEGER | Энергия 0–100 |
| `mood` | INTEGER | Настроение 0–100 |
| `PetInventory` | JSON | Надетый аксессуар |
| `UserInventory` | JSON | Купленные аксессуары |
| `warned_satiety/mood/energy` | INTEGER | Флаги предупреждений |

Колонки `warned_*` добавляются автоматически при первом запуске через `ensure_warn_columns()`.

---

## 🛠️ Команды

| Команда | Описание |
|---------|----------|
| `/start` | Создать питомца или перейти к управлению |
| `/reset` | Удалить текущего питомца |
| `/adm` | Показать ID чата (только для администраторов) |
| `/test` | Сбросить показатели всех питомцев до 10 (только для администраторов) |

---

## 🖼️ Изображения

Все слои — PNG 1000×1000 px с прозрачностью (RGBA). Итоговое изображение сжимается до **512×512 px** для Telegram.

Слои накладываются в порядке: базовый кот → действие → аксессуар → иконки состояния.
